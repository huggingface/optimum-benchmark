defaults:
  - base_config # inherits from base config
  - _self_ # for hydra 1.1 compatibility

experiment_name: cuda_pytorch_inference_bert_2gpu

model: gpt2
device: cuda

backend:
  device_map: auto

hydra:
  job:
    env_set:
      CUDA_VISIBLE_DEVICES: 0,1
