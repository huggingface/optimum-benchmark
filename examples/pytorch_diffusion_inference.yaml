defaults:
  - base_config # inherits from base config
  - _self_ # for hydra 1.1 compatibility

experiment_name: pytorch_diffusion

model: hf-internal-testing/tiny-stable-diffusion-torch
task: stable-diffusion # TasksManager fails to find the task for this model
device: cpu

benchmark:
  warmup_runs: 1
