defaults:
  - base_config # inherits from base config
  - _self_ # for hydra 1.1 compatibility
  - override backend: onnxruntime # override backend to onnxruntime

experiment_name: onnxruntime_diffusion

model: hf-internal-testing/tiny-random-OnnxStableDiffusionPipeline
task: stable-diffusion
device: cpu

backend:
  export: false

benchmark:
  warmup_runs: 1
