hydra:
  mode: MULTIRUN
  sweeper:
    params:
      backend.task: text-generation,feature-extraction
      backend.filename: DistilGPT2-TinyStories.Q4_K_S.gguf
      backend.model: mradermacher/DistilGPT2-TinyStories-GGUF
